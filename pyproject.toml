[project]
name = "paragraphy"
version = "0.1.0"
description = "Yeardream final project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend", "agent", "shared"]
